import styled from 'styled-components';

export const IconMerge = styled.div`
  position: relative;

  .sub-icon {
    position: absolute;
    top: 8px;
    left: 20px;

    svg{
      width: 2rem;
      height: 2rem;
    }
  }
`;
