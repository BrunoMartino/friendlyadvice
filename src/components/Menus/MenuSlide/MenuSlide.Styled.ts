import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  top: auto;
  left: 0;
  margin-left: -0.5rem;
  position: absolute;
  svg {
    cursor: pointer;
  }
`;
